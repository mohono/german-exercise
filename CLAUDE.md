# پروژهٔ آلمانی فاطمه

هدف: آمادگی برای آزمون **ÖSD B2**. این یک کلاس تفننی نیست — فاطمه با آلمانی می‌خواهد دانشگاه برود و کار پیدا کند.

فاطمه **سیستم** می‌سازد، جمله حفظ نمی‌کند. یک قانونِ ناقص کل سیستمش را متزلزل می‌کند.

زبان کار: **فارسی**. جهت تمرین همیشه **فارسی ← آلمانی**.

---

## دو مسیر موازی

| مسیر | چیست | اسکیل | دفتر |
|------|------|-------|------|
| **درس‌ها** | میشل توماس / دویچ ماخت، بلوک‌به‌بلوک | `/anthropic-skills:active-german-lesson` | `lessons/ledger.md` |
| **TED-Ed** | ساخت تمرین یادآوری فعال از ویدیوهای کانال | `/teded-german-drill` | `video-ledger.md` |

**هر درس یا هر ویدیو = یک سشن جدا.** سشن‌ها همدیگر را نمی‌بینند؛ پیوستگی فقط از روی فایل‌های زیر ساخته می‌شود.

---

## 🔴 قانون اول — ابتدای هر سشن

پیش از هر کاری این‌ها را بخوان:

| مسیر درس‌ها | مسیر TED-Ed |
|---|---|
| `claude/وضعیت-درس-*.md` (آخرین شماره) | `video-ledger.md` |
| `claude/سبک-تدریس-و-قالب.md` | `.claude/skills/teded-german-drill/SKILL.md` |
| `claude/gps-osd-b2-master-plan.md` | |
| `claude/workflow-deutsch-macht.md` | |
| `grammar.md` و `vocab.md` | |

اگر فایلی نبود، **بگو که نیست** — از حافظه یا حدس جایش را پر نکن. سابقه دارد که یک قانون ناقص داده شد در حالی که جوابش در جزوهٔ درس ۵ بود.

## 🔴 قانون دوم — انتهای هر سشن

وقتی فاطمه گفت «ببند» (یا جلسه تمام شد):

1. `claude/وضعیت-درس-N.md` را بنویس/به‌روز کن — طبق بخش ۹ اسکیل
2. فایل مرور درس را در `lessons/NN/review.md` بگذار — طبق بخش ۸ اسکیل
3. یک ردیف به `lessons/ledger.md` (یا `video-ledger.md`) اضافه کن
4. قانون‌های تازه را به `grammar.md` و لغت‌های تازه را به `vocab.md` اضافه کن، **با ذکر درس مبدأ**
5. اگر روش تدریس عوض شد، `claude/سبک-تدریس-و-قالب.md` را هم به‌روز کن
6. commit بزن

بدون این شش قدم، جلسهٔ بعد از صفر شروع می‌شود.

## 🔴 قانون سوم — داخل سشن گم نشو

درس را به ۸ تا ۱۲ بلوک بشکن و **در لیست کارها ثبت کن**. هر بلوک که بسته شد تیک بخورد. فاطمه باید هر لحظه ببیند کجای درس است و چقدر مانده.

بلوک آخر همیشه «لغزش‌های خودِ جزوه + جمع‌بندی» است.

---

## ترجمهٔ مسیرها (مهم)

اسکیل `active-german-lesson` در claude.ai نوشته شده و از `project_read` / `project_write` نام می‌برد. **اینجا آن ابزارها وجود ندارند.** معادلشان:

| در اسکیل | اینجا |
|---|---|
| `project_read` | خواندن فایل از همین پوشه |
| `project_write` | نوشتن فایل در همین پوشه |
| `claude/...` | `claude/...` (همین مسیر، نسبت به ریشهٔ پروژه) |
| `TaskCreate` | لیست کارها |

---

## ساختار پوشه‌ها

```
german-exercise/
├── CLAUDE.md              ← همین فایل، هر سشن خودکار لود می‌شود
├── claude/                ← حافظهٔ زندهٔ مسیر درس‌ها
│   ├── وضعیت-درس-N.md        کجا ماندیم، پروفایل خطا، نکته‌های باز
│   ├── سبک-تدریس-و-قالب.md
│   ├── gps-osd-b2-master-plan.md
│   └── workflow-deutsch-macht.md
├── lessons/
│   ├── ledger.md          ← فهرست درس‌ها
│   └── NN/
│       ├── notes.md          جزوهٔ درس
│       └── review.md         فایل مرور (خروجی جلسه)
├── grammar.md             ← قانون‌های تجمیعی، با ارجاع به درس مبدأ
├── vocab.md               ← واژگان تجمیعی
├── anki/                  ← فایل‌های import انکی
│
├── video-ledger.md        ← دفتر TED-Ed
├── scripts/               ← fetch.py (یوتیوب) · drill.py (ساخت HTML + Piper TTS)
├── transcripts/           ← متن خام آلمانی، <videoId>.txt
├── sources/               ← فایل منبع واحدبندی‌شده، NNN-Slug.txt
└── texts/                 ← خروجی نهایی، HTML مستقل
```

`grammar.md` و `vocab.md` چیزی‌اند که دو مسیر را به هم وصل می‌کنند: وقتی درس ۹ به Dativ برمی‌گردد، باید دیده شود درس ۳ چه گفته بود و به‌جای تکرار، وصل شود.

---

## محیط اجرا

- ویندوز، PowerShell. `python3` کار می‌کند.
- `drill.py build` نیاز دارد: `piper-tts`، مدل صدای `de_DE-thorsten-medium` در `~/.cache/piper-voices/`، و `ffmpeg.exe` + `node.exe` در `C:\Users\FATEMEH\bin` — **این پوشه در PATH نیست و باید هنگام build اضافه شود**.
- جزئیات بیشتر در یادداشت‌های `video-ledger.md`.
